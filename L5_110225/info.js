//Set time-out 
// For a particular time period it will stay and when the time is complete it will join the execution thread.

// set interval

// For a particular time it will stay in the queue and then join the thread and this happens again and again


// A closure is the combination of a function bundled together (enclosed) with references to its surrounding state (the lexical environment). In other words, a closure gives a function access to its outer scope. In JavaScript, closures are created every time a function is created, at function creation time.